habituacao = bool(input("O animal está habituado: "))

som = 0
lado = 0

agua = 0
tentativa = 0

tempo = 0
tentativa2 = 0

if(habituado):
   aproximacao = 30
   
   aproximacao = int(input("O animal se aproximou quanto? "))
   tentativa = int(input("Qual o numero da tentativa atual? "))
   
   if(aproximacao < 30 and tentaviva <= 20):
      agua = 0.5
   agua = 0.0
      
   if(tentativa > 20): 
      print("O primata esta apto para a proxima fase")

   aproximacao = 30   
   aproximacao = int(input("O animal se aproximou quanto? "))
   tentativa = int(input("Qual o numero da tentativa atual? "))
   
   if(aproximacao < 30 and tentaviva <= 20)
      agua = 0.5
   agua = 0.0
   if(tentativa > 20): 
      print("O primata esta apto para a proxima fase")
            
   aproximacao = 30   
   aproximacao = int(input("O animal se aproximou quanto? "))
   tentativa = int(input("Qual o numero da tentativa atual? "))
   
   if(aproximacao < 30 and tentaviva <= 20):
      agua = 0.5
   agua = 0.0
   
   if(tentativa >= 20): 
      print("O primata esta apto para a proxima fase")   
   
      print("Iniciando 2a fase")
      
      agua = 0.0
      som = int(input("Qual o som escutado, 1 ou 2?"))
      lado = int(input("Qual lado escolhido pelo primata, 1-esquerda ou 2-direita?"))
      
      if((som == 1 and lado == 1) or (som == 2 and lado == 2)):
         agua = 0.5
         som = 0
         lado = 0
      
      agua = 0.0
      som = int(input("Qual o som escutado, 1 ou 2?"))
      lado = int(input("Qual lado escolhido pelo primata, 1-esquerda ou 2-direita?"))
      
      if((som == 1 and lado == 1) or (som == 2 and lado == 2)):
         agua = 0.5
         som = 0
         lado = 0
      agua = 0.0
      som = int(input("Qual o som escutado, 1 ou 2?"))
      lado = int(input("Qual lado escolhido pelo primata, 1-esquerda ou 2-direita?"))
      
      if((som == 1 and lado == 1) or (som == 2 and lado == 2)):
         agua = 0.5
         som = 0
         lado = 0
      tempo = int(input("Qual foi a duração do experimento, em minutos?"))
      tentiva2 = int(input("Quantas tentativas foram executadas?"))
      
      if(tempo >= 30 and tentativa2 >= 50):
         print("O primata esta apto para a proxima fase")  
      else:
         print("O primata nao esta apto para a proxima fase")  
   else:
      print("O primata nao esta apto para a proxima fase")  
else: 
   print("O animal nao esta habituado")
